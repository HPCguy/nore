// Error: match arms produce incompatible types
enum Option { Some(i64), None }

func main(): void = {
    val x: Option = Option.Some(1)
    val v: i64 = match (x) {
        Some(n) = { n }
        None = { true }
    }
}
