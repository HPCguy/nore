import helper "helper.nore"

native func mem_copy(mut ref dst: [u8], ref src: [u8]): i64

func ping(): i64 = {
    return helper.ping()
}

func use_pair(ref pair: helper.Pair): void = {
}

func touch(ref data: [u8], mut ref count: i64): void = {
}
